- name: My first play
  hosts: all
  strategy: free
  tasks:
   - name: Ping my hosts
     ansible.builtin.ping:
     ignore_unreachable: yes
   - name: Print message
     ansible.builtin.debug:
      msg: Hello world
